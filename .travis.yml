sudo: required
language: node_js
node_js:
    - "node"
jobs:
    include:
        - stage: ChattyWebclient
          before_install:
            - cd src/main/resources/public/assets
          install:
            - yarn
          script: 
            - yarn start
        - stage: ChatyService
          install:
            - sudo apt install -y groovy
          script:
            - ./gradlew build
